<view class="body">
    <view class="lose-list">
        <ViewCard wx:for="{{ list }}" wx:key="index" data="{{ item }}" bindtap="toDetail" data-info="{{ item }}" showsimilar="{{ true }}" similar="{{item.similar}}" />
        <canvas type="2d" id="canvas1" class="canvas"></canvas>
        <canvas type="2d" id="canvas2" class="canvas"></canvas>
    </view>
</view>